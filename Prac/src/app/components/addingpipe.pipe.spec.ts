import { AddingpipePipe } from './addingpipe.pipe';

describe('AddingpipePipe', () => {
  it('create an instance', () => {
    const pipe = new AddingpipePipe();
    expect(pipe).toBeTruthy();
  });
});
